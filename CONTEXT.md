# FormEcho

自然言語入力を AI で解析し、画面フォームの構成に合わせた構造化データを返す仕組みの検証環境。参照アーキテクチャ（AgentCore Runtime を BFF から分離する構成）を実際に動かして、プロンプト・Guardrail の精度と本番向けサンプルコードを得ることを目的とする。

## Language

### 環境

**デプロイ済み検証環境**:
AWS 上に置いた検証環境の実体。ローカルの3プロセスと同じ入出力契約を満たすが、載せ方は
参照アーキテクチャとは別（ADR-0014）。
_Avoid_: 検証環境（単独。このリポジトリが作っているもの全体を指す）、本番、ステージング

### エージェント基盤

**Runtime**:
AgentCore Runtime。デプロイの単位であり、この検証環境には1つだけ存在する。
_Avoid_: エージェント（単独で使うと曖昧）

**ドメインエージェント**:
1つのドメイン（交通IC、会議ロジ）に対応する Strands の `Agent` インスタンス。ドメイン間で協調しない。
_Avoid_: エージェント（単独）、サブエージェント、コラボレーター

**Skill**:
`SKILL.md` 1ファイル。ドメインエージェントが持つ振る舞いの単位で、Strands の `AgentSkills` がロードする。
_Avoid_: プロンプト、タスク、テンプレート

**Agent Skills（ハーネス）**:
このリポジトリの `.claude/` が使う Claude Code 側の仕組み。上の Skill とは無関係で、同じ語で呼ぶと必ず混乱する。
_Avoid_: Skill（修飾なし）

**taskId**:
BFF から Runtime へ渡すルーティングキー。どのドメインエージェントを使うかを決める。
_Avoid_: 機能ID、タスク名、機能名

**BFF**:
`hono-app`。入力検証と Runtime 呼び出しを担う層。
_Avoid_: API、バックエンド、プロキシ

### 入出力

**出力契約**:
Runtime が返す構造化データのスキーマ。`agent-app` / `hono-app` / `nextjs-app` がそれぞれ
自己完結の Zod スキーマとして持つ（共有ディレクトリは無い。ADR-0011）。
_Avoid_: レスポンス型、スキーマ（単独）、Structured Output（機能名であって契約ではない）

**応答封筒**:
BFF が画面へ返す JSON のうち、**出力契約**の外側にある取り決め。`sessionId`・`usage`・`citations`・
エラーの `code` と `message` からなる。AI は関与せず、BFF と画面の2者だけで決まる。
**この1辺だけは複製ではなく Hono RPC の `AppType` で型が届く**（ADR-0015）。
_Avoid_: 契約（単独。出力契約と紛れる）、レスポンス（単独）、メタデータ（`citations` は表示義務が掛かる本体であって付随情報ではない）

**出典**:
Runtime が Web 検索で実際に取得した Search Result の出どころ。ページのタイトルとリンクから
なり、**応答封筒**の `citations` で運ばれる。**画面に出す正典はこちら。**
_Avoid_: 参照元（あちらはモデルの申告で正典ではない）、根拠、引用元

**参照元**:
AI が「回答の根拠にした」と応答に書いた URL の一覧（**出力契約**の `sources`）。モデルの申告
なので、使ったのに載せないことも、取得していない URL を混ぜることもある。**表示の正典では
ない。**
_Avoid_: 出典（あちらは Runtime が実際に取得したもので、こちらとは別物）、ソース、引用

**Guardrail チェック**:
`InvokeGuardrailChecks` API による入力・出力の検査。`contentFilter` / `promptAttack` / `sensitiveInformation` の3種からなる。
_Avoid_: ガードレール（リソースとしての Guardrail と紛れる）、入力検証（BFF の長さ・形式チェックと紛れる）

**日本固有 PII 検知**:
**Guardrail チェック**が持たない日本の識別子を、Runtime のコード内の正規表現で検知する層。AWS の PII タイプは米英加・汎用のものだけで日本の識別子に対応する型が無いため、恒久的にこちら側で持つ。現在の対象はマイナンバー。
_Avoid_: マイナンバー検知（対象が増えたら語が壊れる。マイナンバーはこの境界に落ちた最初の1つ）、カスタム正規表現（Guardrail リソース側の `regexesConfig` と紛れる）、PII 追加検知（「追加」が相対的で、Guardrail チェック側が将来対応すると意味を失う）

**severityScore**:
コンテンツがチェック基準にどれだけ強く該当するかの度合い。`contentFilter` と `promptAttack` が返す。モデルの確信度ではない。
_Avoid_: 信頼度、確度

**confidenceScore**:
その PII が存在するというモデルの確信度。`sensitiveInformation` が返す。
_Avoid_: スコア（単独。severityScore と意味が違う）

**非AI経路**:
AI を使わずにフォームを埋めきれる経路。すべての AI 機能に対して確保される。
_Avoid_: 手動入力（フォールバックとしての手動入力と、初めから AI を使わない選択の両方を指してしまう）

### 交通IC

**移動経路**:
ICカード予約における**最寄**から**最寄**までの交通ルート（例:「霞ケ関駅(東京メトロ日比谷線) => 虎ノ門駅」）。鉄道とバスを含む。区間ごとの構造は持たず、AI が整形した1本の文字列として持つ。
_Avoid_: 経路（単独。**非AI経路**と衝突する — あちらは画面遷移の経路であって交通ルートではない）

**移動経路候補**:
出発地から目的地までの**移動経路**の候補ひとつ。比較の土台に乗った合理的なものだけが候補になる（所要時間が最短の1.5倍を超える経路・乗換が最少より2回以上多い経路は候補にしない）。候補は必ず**経路検索結果**を1つ指す。
_Avoid_: 経路候補（`経路` 単独と同じ理由）、他候補（採用したものを含められない）

**採用移動経路**:
**移動経路候補**のうち、比較検討の末に採用した1本。候補が1件以上あるときちょうど1つ決まる。
_Avoid_: 採用経路（`経路` 単独と同じ理由）、最安経路（同額のときは所要時間・乗換回数で決まるので最安とは限らない）

**最寄**:
出発地・目的地それぞれを、運賃計算の起点・終点として解決した駅またはバス停。**入力が既に駅名でも返す** — 運賃がどの区間の額なのかは常に画面から読めている必要がある。
_Avoid_: 最寄駅（バス停を含められない）、最寄停留所（駅を停留所と呼ぶことになる）

**1人あたり運賃（見込み）**:
**採用移動経路**のIC運賃を1人分で表した額。**往復区分**が往復なら往復分（片道の2倍）。「見込み」は額が概算であることを許す語で、書式が緩いという意味ではない。
_Avoid_: 交通費（1人分か全員分かを言えていない）、運賃（単独。片道か往復か、誰の分かが落ちる）

**往復区分**:
片道か往復かの2択。**1人あたり運賃（見込み）**を片道分にするか往復分にするかを決める。
_Avoid_: 片道往復（語として読みにくい）、往復フラグ（2値ではなく2択の選択肢）、区分（単独）

**出発日時**:
移動を始める日時。**運賃・経路の計算には使わない**（通常運賃・平日ダイヤを前提にするため）ので、申請に載せる情報としてだけ持つ。
_Avoid_: 利用日時（カードの貸借の日時と紛れる）、借りる日（カードを受け取る日であって移動の日ではない）

**定期区間**:
職員が持つ通勤定期券の区間。フォームの構造化入力には無く、自由文で伝えられたときだけ AI が読み取る（#101）。
_Avoid_: 定期（単独。定期券という物を指すのか区間を指すのか曖昧）

**定期重複区間**:
**移動経路候補**ひとつのうち、**定期区間**と重なる駅間。連続区間ごとに数える。自由文に**定期区間**の言及が無ければ区間そのものが不明なので、候補との重複が無いのとは区別して null で持つ。
_Avoid_: 重複区間（単独。何との重複か分からない）

**経路検索結果**:
**移動経路候補**1件を引いた検索結果のページ。候補と1対1で対応し、職員が運賃と経路の根拠を
その場で開いて確かめられるようにする。**AI は URL ではなく出典の番号で指す**（ADR-0019）。
_Avoid_: 出典（応答全体のものを指してしまう）、参照元（AI の申告と紛れる）、エビデンス、検索結果（単独）

### 会議ロジ

**候補日程**:
会議の開催日時の候補ひとつ。識別子と開始日時を持つ。終わる時刻は候補日程ごとには持たず、会議の**所要時間**から導く。
_Avoid_: 候補（単独だと、AI が提案したものなのか参加可否を問う対象なのか区別が付かない）、スロット（カレンダー上の30分の升目であって候補日程ではない）

**スロット**:
候補日程を選ぶカレンダー上の30分の升目。候補日程の**表示単位**であって選択単位ではない — 職員が1回クリックすると、所要時間ぶんの連続したスロットが1つの候補日程になる。
_Avoid_: コマ、枠、候補日程

**参加形式**:
会議への出席のしかた。ハイブリッド・現地のみ・オンラインのみの3つ。会議ごとに1つ決まり、参加者に見せる参加可否の選択肢を決める。
_Avoid_: 開催形式、会議形式、フォーマット

**参加可否**:
候補日程ひとつに対する参加者の答え。現地で出席・リモートで出席・欠席・未定の4つ。参加形式が現地のみ／オンラインのみの会議では、出席は1通りしかないので選択肢は3つに畳まれる。
_Avoid_: 出欠、可用性、availability、○×（4状態になったので2値を示唆する表記は使わない）

**未定**:
参加者が答えたが、出席するか決まっていない状態。**未回答とは違う** — 未定は回答であり、未回答は回答の不在。
_Avoid_: 保留、未確定、未回答

**備考**:
参加可否ひとつに添える自由記述。「午前中は別の予定がある」のような、4つの選択肢に収まらない事情を書く。
_Avoid_: コメント、メモ、注釈

**職員**:
画面を操作する人。フォームを埋め、AI に指示を出し、AI の出力を直す主体。
_Avoid_: ユーザー、利用者（参加者と紛れる）

**参加者**:
会議への出席を問われる人ひとり。候補日程に対して参加可否を答える主体で、職員とは別。
_Avoid_: メンバー、出席者（出席が確定した人に読める）、ユーザー

**参加可否表**:
参加者 × 候補日程 に参加可否を張った表。未回答はセルが存在しないことで表す。
_Avoid_: 候補日リスト、候補日（時刻を落とすので候補日程とずれる）、マトリクス、集計

**候補日提案**:
参加可否表を踏まえて全候補日程に付けた評点と根拠。職員が開催日を決めるための叩き台であって、決定ではない。
_Avoid_: 推薦（単独）、ランキング、決定

**AI評価ラベル**:
候補日提案の評点を職員に見せるための区分。推奨・予備に提案・要検討・条件合わず・参加入力未済の5つ。評点から機械的に導かれるもので、AI が直接選ぶものではない。
_Avoid_: 判定、評価（単独）、ステータス

**開催日**:
会議を実際に開く候補日程。全候補日程からちょうど1つ選ぶ。
_Avoid_: 確定日、本命

**予備日**:
開催日が使えなくなったときのために確保しておく候補日程。0個以上選べる。開催日に選んだ候補日程は予備日にできない。
_Avoid_: バックアップ、次点、代替日

**AI入力アシスタント**:
各画面の上部に置く、自然文から AI が値を作るセクション。折りたためる。この下に区切り線を挟んで**非AI経路**のフォームが同じ画面に並ぶ。
_Avoid_: AIチャット、チャット欄、アシスタント（単独）

**プレビュー**:
AI が返した結果を、フォームに書き込む**前に**職員へ見せる表示。ここで確認して「反映」を押すまでフォームは変わらない。
_Avoid_: 結果、確認画面、ドラフト

**AI提案の内訳**:
**プレビュー**の中で、AI が**何を調べてなぜそれを選んだか**を見せる領域。欄と対応しないので抽出項目の一覧とは別に置く（混ぜると聞き返しの判定の分母に欄でない行が入る）。中身を持つのは交通ICだけで、空のときは領域ごと出さない。
_Avoid_: 根拠（**出典**と紛れる）、検索結果（単独。**出典**そのものを指してしまう）
